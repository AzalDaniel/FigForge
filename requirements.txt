# Core (forensics + image ops + effects)
Pillow>=10
numpy>=1.24
# Rasterising SVG layers / rendering reference SVGs (needs system libcairo)
cairosvg>=2.7
# HTML email -> PNG verification (needs system libpango+libcairo); PDF rasterised by pymupdf
weasyprint>=60
pymupdf>=1.23
